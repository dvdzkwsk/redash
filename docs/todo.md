* [ ] Efficient build system that doesn't require loading modules at run time.
* [ ] Component-level tests that can be re-run in isolation.
* [ ] Expose lib properly based on environment (CommonJS, AMD, web).
