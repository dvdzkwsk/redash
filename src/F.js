import always from './always'

/**
 * @name F
 * @category Function
 * @since v0.16.0
 * @description
 * Always returns false; equivalent to `always(false)`.
 * @see T
 *
 * @example
 * F() // => false
 */
export default always(false)
