/**
 * @name length
 * @signature [a] -> Number
 * @since v0.13.0
 * @description
 * Returns the length of a list.
 *
 * @example
 * length([1, 2, 3, 4, 5]) // => 5
 */
export default function length (xs) {
  return xs.length
}
