/**
 * @name inc
 * @category Math
 * @since v0.6.0
 * Increments a number by `1`.
 * @see dec
 *
 * @example
 * inc(10) // => 11
 * inc(-1) // => 0
 */
export default function inc (n) {
  return n + 1
}
