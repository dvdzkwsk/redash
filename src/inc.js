/**
 * @name inc
 * @signature Number -> Number
 * @since v0.6.0
 * Increments a number by `1`.
 * @see dec
 *
 * @example
 * inc(10) // => 11
 * inc(-1) // => 0
 */
export default function inc (a) {
  return a + 1
}
