/**
 * @name trim
 * @category String
 * @since v0.13.0
 * @description
 * Returns the string with all leading and trailing whitespace removed.
 *
 * @example
 * trim('  hello  ') // => 'hello'
 */
export default function trim (a) {
  return a.trim()
}
